# Architecture

One page. If you read only this, you should be able to find any behaviour in the repo.

## What this is

<!-- Written by hand. What problem does this repo solve, and what is the data flow?
     The generated map below says what exists; this section says why. -->

## Where to start reading

<!-- Name the one or two files that explain the rest. -->

## File map

<!-- FILEMAP:START - generated by docmap.py, do not edit by hand -->

### Directories

| Path | Source files |
|---|---:|
| `(repo root)` | 1 |
| `.claude/hooks` | 1 |
| `mcp_servers/plan_review` | 1 |
| `scripts` | 16 |
| `src/coilforge` | 4 |
| `src/coilforge/accuracy` | 2 |
| `src/coilforge/adapters` | 3 |
| `src/coilforge/ambient` | 13 |
| `src/coilforge/capture` | 9 |
| `src/coilforge/ccsi` | 3 |
| `src/coilforge/checklist` | 8 |
| `src/coilforge/coil_utilities` | 4 |
| `src/coilforge/common` | 2 |
| `src/coilforge/compatibility` | 10 |
| `src/coilforge/contracts` | 4 |
| `src/coilforge/deliverable` | 3 |
| `src/coilforge/direct_coil` | 7 |
| `src/coilforge/drawing` | 9 |
| `src/coilforge/drawing/backends` | 2 |
| `src/coilforge/interfaces` | 1 |
| `src/coilforge/interfaces/direct_coil` | 2 |
| `src/coilforge/package` | 4 |
| `src/coilforge/phase2a` | 10 |
| `src/coilforge/reconciliation` | 1 |
| `src/coilforge/review` | 7 |
| `src/coilforge/rules` | 3 |
| `src/coilforge/schemas` | 2 |
| `src/coilforge/services` | 9 |
| `src/coilforge/submittal` | 11 |
| `src/coilforge/template_population` | 4 |
| `src/coilforge/validation` | 2 |
| `src/coilforge/workflows` | 3 |
| `tests` | 126 |
| `tests/fixtures/po_logic/PO Release Engineering Workflow/pdf_extractor` | 2 |
| `web` | 1 |
| `web/ccsi` | 1 |

### Files

| File | Purpose |
|---|---|
| `test_case_journal.py` | Standalone check for src/coilforge/case_journal.py (run: python test_case_journal.py) |
| `.claude/hooks/run_drawing_tests.py` | Claude Code PostToolUse hook: run the drawing-engine tests after a relevant edit |
| `mcp_servers/plan_review/server.py` | plan-review MCP server: one tool, ``review_plan``, backed by OpenAI |
| `scripts/draw_audit_sample.py` | Draw a flag-INDEPENDENT random audit sample (1d): pick N coils for ground-truth review, |
| `scripts/execute.py` | Harness Step Executor — phase 내 step을 순차 실행하고 자가 교정한다 |
| `scripts/find_similar.py` | Case Retrieval (Stage 2): "have I seen this coil before?" |
| `scripts/generate_coverage_dashboard.py` | Generate the template-coverage dashboard from the live drawing catalog |
| `scripts/inventory_ventum_selection.py` | Phase 0 (READ-ONLY): inventory the Ventum+ coil-selection PDFs page-by-page so we |
| `scripts/override_rate.py` | Review Triage measurement (Stage 3.0): "which fields does John actually override?" |
| `scripts/preview_schematic.py` | Eyeball gate for the parametric drawing engine (Phase 1 / 1.5 / 2) |
| `scripts/promote_divergences.py` | Promote adjudicated divergences from the working file into the tracked registry |
| `scripts/render_dx_lh_pngs.py` | Render populated DX_1_LH and DX_2_LH template drawings to PNG (review aid) |
| `scripts/render_slot_label_previews.py` | Render all 22 drawing templates with each slot filled by its own slot-id label |
| `scripts/render_template_drawing_preview.py` | Render the template-drawing preview panel to a self-contained HTML file |
| `scripts/render_value_label_previews.py` | Render all 22 drawing templates populated with REAL engine values + inline labels |
| `scripts/replay_run.py` | Time Machine (1d): re-run the rule engine for a stored capture run and compare to the |
| `scripts/rule_observatory.py` | Rule Observatory (Stage 4.0): "which RULE keeps producing a value somebody disagreed with?" |
| `scripts/seed_templates_from_pdf.py` | Seed drawing templates from the real CoilMaster EZ Coil drawing PDFs |
| `scripts/tune_case_weights.py` | Case Retrieval weight-tuning harness (Stage 2, Part A5) |
| `src/coilforge/__init__.py` | CoilForge local MVP package |
| `src/coilforge/brain_case.py` | PO Release Case reader (case prefill deep link, v0) |
| `src/coilforge/case_journal.py` | PO Release Case journal writer (write-hook, v0) |
| `src/coilforge/accuracy/__init__.py` | Accuracy regression helpers for the Phase 2B MVP workflow |
| `src/coilforge/adapters/__init__.py` | Compatibility adapters that feed CoilForge canonical records |
| `src/coilforge/ambient/__init__.py` | Ambient Dynamics coil-supplier extension (quick-ship quote comparison) |
| `src/coilforge/ambient/coil_volume.py` | Coil internal-volume formula — re-exported from the canonical Coil Utilities home |
| `src/coilforge/ambient/compare.py` | Per-field tolerance policy + verdict stamping for the Ambient comparison |
| `src/coilforge/ambient/excel_map.py` | Pure mapping + model for the Ambient comparison Excel write-back (no I/O) |
| `src/coilforge/ambient/excel_writer.py` | Excel writer for the Ambient comparison workbook (I/O layer — Windows + Excel COM) |
| `src/coilforge/ambient/mapping.py` | Project baseline + Ambient candidates -> a per-coil comparison (pure) |
| `src/coilforge/ambient/model.py` | Pure data model for an Ambient-vs-baseline coil comparison (no I/O) |
| `src/coilforge/ambient/package.py` | Pure builder for a submittal -> Ambient "performance page + drawing" package (no I/O) |
| `src/coilforge/ambient/pdf_intake.py` | Parse an Ambient Dynamics "Coils Performance & Drawings" PDF into candidates |
| `src/coilforge/ambient/quote_request_pdf.py` | Ambient Dynamics quote-request PDF — the paper artifact John hands the supplier |
| `src/coilforge/ambient/range_provider.py` | Coil-Utilities-backed acceptance-range provider for the Ambient comparison |
| `src/coilforge/ambient/rfq.py` | Build a structured performance-target summary to send Ambient for a quote (Feature 1) |
| `src/coilforge/ambient/units.py` | Unit normalization for Ambient performance data |
| `src/coilforge/capture/__init__.py` | CoilForge capture ledger — append-only SQLite store of what each run produced |
| `src/coilforge/capture/db.py` | Capture-ledger infrastructure — connection, migrations, guards, kill switch |
| `src/coilforge/capture/observatory.py` | Rule Observatory (Stage 4.0): read-only, per-RULE measurement over the ledger |
| `src/coilforge/capture/observe.py` | Capture-ledger observability (1d): read-side tools over the append-only ledger |
| `src/coilforge/capture/record.py` | result dict -> ledger rows |
| `src/coilforge/capture/retrieve.py` | Case retrieval (Stage 2.0): read-only nearest-neighbor over the capture ledger |
| `src/coilforge/capture/schema.py` | Capture-ledger schema — forward-only, additive-only migrations (1a) |
| `src/coilforge/capture/triage.py` | Review-triage measurement (Stage 3.0): read-only override-rate over the ledger |
| `src/coilforge/capture/tuning.py` | Case-retrieval weight tuning harness (Stage 2, Part A5) |
| `src/coilforge/ccsi/__init__.py` | CCSI Direct Coil integration (server side) |
| `src/coilforge/ccsi/compare.py` | Compare CoilForge drawing-parameter values against values read back from the |
| `src/coilforge/ccsi/export_audit.py` | Offline CCSI-export audit — run the green/red compare from a downloaded CCSI |
| `src/coilforge/checklist/__init__.py` | Coil Checklist auto-fill subsystem (review aid) |
| `src/coilforge/checklist/compare.py` | Build the in-app review table: checklist (formula) vs CoilForge (engine) |
| `src/coilforge/checklist/excel_writer.py` | Excel writer for the Coil Checklist (I/O layer — Windows + Excel COM only) |
| `src/coilforge/checklist/from_workflow.py` | Adapter: submittal candidates -> checklist coil-input dicts |
| `src/coilforge/checklist/mapping.py` | Pure mapping: submittal coils -> ``ChecklistFill`` (no Excel, no I/O) |
| `src/coilforge/checklist/model.py` | Pure data model for a Coil Checklist fill (no I/O, no Excel) |
| `src/coilforge/checklist/overrides.py` | Pure adapter: the engineer's browser manual fills -> checklist inputs/dims |
| `src/coilforge/checklist/template_map.py` | Structural reference for "Coil Checklist Template.xlsx" (pure data, no I/O) |
| `src/coilforge/coil_utilities/__init__.py` | Coil Utilities — Oxygen8's internal coil data charts, ported to a reusable table |
| `src/coilforge/coil_utilities/charts.py` | Raw per-kit source charts, transcribed from the Coil Utilities workbook |
| `src/coilforge/coil_utilities/geometry.py` | Coil geometry engine — the left-block calculators shared across the workbook |
| `src/coilforge/coil_utilities/ranges.py` | Kit lookup + acceptance ranges scaled by circuits, and the Allowable Ranges criteria |
| `src/coilforge/common/__init__.py` | Small cross-cutting helpers shared by otherwise independent CoilForge packages |
| `src/coilforge/common/excel_lock.py` | Single-flight guard for the Excel COM writers (in-process + cross-process) |
| `src/coilforge/compatibility/__init__.py` | Review-only compatibility comparison helpers |
| `src/coilforge/compatibility/mechanical_fit.py` | Mechanical fit / coil-stability checks (review aid) |
| `src/coilforge/contracts/__init__.py` | Shared CoilForge contract models |
| `src/coilforge/deliverable/__init__.py` | DirectCoil deliverable finalization — file the docs + draft the hand-off email |
| `src/coilforge/deliverable/finalize.py` | Pure / filesystem layer for finalizing a DirectCoil deliverable |
| `src/coilforge/deliverable/outlook_draft.py` | Outlook draft I/O (Windows + Outlook COM only) |
| `src/coilforge/direct_coil/__init__.py` | Direct Coil draft models and mappers |
| `src/coilforge/direct_coil/page_reader.py` | Read values a human entered into the CCSI 'Direct Coil' web form |
| `src/coilforge/direct_coil/verify.py` | Compare a human's Direct Coil web entry against CoilForge's canonical record |
| `src/coilforge/drawing/__init__.py` | Drawing preview parameter policy models |
| `src/coilforge/drawing/label_authority.py` | Direct Coil label authority for drawing dimension callouts |
| `src/coilforge/drawing/schematic_layout.py` | Layer 2 — layout / datum engine (real inches only, no pixels) |
| `src/coilforge/drawing/schematic_model.py` | Layer 1 — geometry model for the parametric drawing engine (real inches only) |
| `src/coilforge/drawing/schematic_renderer.py` | Orchestrator — gated slot_values -> to-scale DX front + side SVGs (review aid) |
| `src/coilforge/drawing/backends/__init__.py` | Renderer backends for the parametric drawing engine |
| `src/coilforge/drawing/backends/svg.py` | Layer 3 — SVG backend (the only place pixels exist) |
| `src/coilforge/interfaces/__init__.py` | Software-specific CoilForge interface contracts |
| `src/coilforge/interfaces/direct_coil/__init__.py` | Direct Coil interface field model |
| `src/coilforge/package/__init__.py` | Outgoing drawing-package assembly (quote-prep review aid) |
| `src/coilforge/package/assembler.py` | Assemble the outgoing drawing package (quote-prep review aid) |
| `src/coilforge/package/copper_strap_pricing.py` | Copper-strap price adder for the outgoing quote package |
| `src/coilforge/package/svg_to_pdf.py` | SVG -> PDF print/compose utility (NOT a geometry renderer backend) |
| `src/coilforge/phase2a/__init__.py` | Phase 2A local web MVP package |
| `src/coilforge/reconciliation/__init__.py` | Source reconciliation policy entrypoints |
| `src/coilforge/review/__init__.py` | Phase 2D quote-prep review packet helpers |
| `src/coilforge/review/adjudicate.py` | Record a human ruling on a divergence: ledger row + staging-registry entry |
| `src/coilforge/review/divergence.py` | Known-divergence registry: John's rulings on checklist-vs-engine disagreements |
| `src/coilforge/review/project_gate.py` | Constant-time project review gate — collapse per-coil review into an |
| `src/coilforge/review/rule_proposal.py` | Draft a rule-change proposal from an adjudicated divergence. Never applies one |
| `src/coilforge/rules/__init__.py` | Centralized CoilForge mapping rule registry |
| `src/coilforge/schemas/__init__.py` | Pydantic schemas for CoilForge services |
| `src/coilforge/schemas/header_prepopulate.py` | Pydantic v2 models for the deterministic header prepopulation engine |
| `src/coilforge/services/__init__.py` | CoilForge application services |
| `src/coilforge/services/direct_coil_drawing_pipeline.py` | Track A pipeline: coil inputs -> prepopulated header -> populated draft drawing |
| `src/coilforge/services/distributor_slots.py` | Phase 4a — UPSTREAM sourcing of the V3 distributor drawing slots (no drawing) |
| `src/coilforge/services/drawing_param_resolver.py` | Generate drawing preview parameters from the header rule engine |
| `src/coilforge/services/ez_json_drawing_loader.py` | Apply the JSON -> drawing link registry to an EZ Coil export |
| `src/coilforge/services/header_prepopulate_engine.py` | Deterministic header prepopulation rule engine |
| `src/coilforge/services/json_drawing_link.py` | Loader for the JSON -> drawing link-rule registry |
| `src/coilforge/services/template_selection.py` | Link an extracted coil selection to its drawing template |
| `src/coilforge/services/three_way_view.py` | Three-way review view (Phase 2b): for one coil, put the three sources of a field's |
| `src/coilforge/submittal/__init__.py` | Submittal candidate contract models |
| `src/coilforge/submittal/coilmaster_drawing_extract.py` | Extract coil data from CoilMaster drawing PDF text + the submittal cover page |
| `src/coilforge/submittal/model_code.py` | Read the drain-pan option out of an Oxygen8 unit model code. Pure, no I/O |
| `src/coilforge/submittal/pdf_to_template_drawing.py` | PDF -> coil type -> template link -> logic-derived, populated drawing |
| `src/coilforge/template_population/__init__.py` | Template-first drawing population helpers |
| `src/coilforge/template_population/mirror.py` | Reusable LH <-> RH drawing-template mirror for the PDF-derived templates |
| `src/coilforge/validation/__init__.py` | CoilForge validation rule helpers |
| `src/coilforge/workflows/__init__.py` | Backend workflow orchestration helpers |
| `src/coilforge/workflows/drawing_package.py` | Drawing-package workflow: combine the Direct Coil drawing with ours |
| `tests/conftest.py` | Suite-wide isolation for the capture ledger |
| `tests/test_ambient_coil_volume.py` | Phase 1 — Ambient coil-volume formula port (pure; no I/O) |
| `tests/test_ambient_compare.py` | Phase 4 — Ambient comparator tolerance + the shared _match regression guard |
| `tests/test_ambient_excel.py` | Ambient comparison Excel write-back — pure mapping + route contract |
| `tests/test_ambient_mapping.py` | Phase 4 — build_ambient_comparison pairing, ranges, and loud unmatched coils |
| `tests/test_ambient_package.py` | Stage — Ambient submittal->package builder (transcription + acceptance band) |
| `tests/test_ambient_pdf_intake.py` | Phase 2 — Ambient PDF parser (text-level; sanitized fixtures, no committed PDF) |
| `tests/test_ambient_quote_request_pdf.py` | Phase 5 — the Ambient quote-request PDF (builder + /api/ambient/quote-request-pdf) |
| `tests/test_ambient_range_provider.py` | Phase 3 wiring — the Coil-Utilities range provider drives real acceptance verdicts |
| `tests/test_ambient_web.py` | Phase 5 — /api/ambient/* route contract (TestClient; parsing monkeypatched) |
| `tests/test_capture_ledger.py` | Capture ledger (1a): every coil selection's (inputs -> proposal -> correction) |
| `tests/test_capture_observe.py` | Capture-ledger observability (1d): run_dedup view, /api/capture/health, the |
| `tests/test_capture_retrieve.py` | Case retrieval (Stage 2.0): masked-Gower nearest-neighbor over the ledger |
| `tests/test_case_prefill.py` | PO Release board case prefill (?case= deep link) |
| `tests/test_case_tuning.py` | Case-retrieval weight tuning harness (Stage 2, Part A5) |
| `tests/test_ccsi_compare.py` | CCSI-vs-CoilForge compare core (Phase 3): reuses the checklist comparator, so a |
| `tests/test_ccsi_export_audit.py` | Offline CCSI-export audit (`ccsi/export_audit.py`) |
| `tests/test_ccsi_field_map.py` | Contract guard for the CCSI Direct Coil autofill field map |
| `tests/test_checklist_cache.py` | Bounded sha1 memoization of the Coil Checklist fill (_run_or_reuse_checklist) |
| `tests/test_checklist_compare.py` | Phase 3 — adapter + comparison tests (pure; no Excel, no PDF) |
| `tests/test_checklist_excel_writer.py` | Phase 2 — Excel writer integration test (Windows + Excel only) |
| `tests/test_checklist_from_workflow.py` | Tests for the candidate -> coil-input adapter: per-coil product detection |
| `tests/test_checklist_ledger_signal.py` | Checklist divergences reach the review-triage ranking (Stage 3.1 groundwork) |
| `tests/test_checklist_mapping.py` | Phase 1 — pure checklist mapping tests (no Excel) |
| `tests/test_checklist_overrides.py` | Manual browser fills carried into the Coil Checklist (pure; no Excel, no PDF) |
| `tests/test_checklist_panel_join.py` | The Drawing Parameters panel joins the Coil Checklist by SLOT, not by name |
| `tests/test_checklist_partner_cd.py` | The partner CD written onto a sheet must equal that partner's OWN sheet CD (pure) |
| `tests/test_circuit_from_coil_style.py` | Circuit count derived from the 'Coil Style' prose |
| `tests/test_coating_note_on_drawing.py` | The coil's ACTUAL coating is printed on the drawing (John 2026-08-05) |
| `tests/test_coil_utilities.py` | Phase 3 — Coil Utilities internal table (geometry engine + capacity-range charts) |
| `tests/test_coilmaster_drawing_extract.py` | Tests for CoilMaster drawing + cover-page extraction (synthetic text) |
| `tests/test_condensing_mirror_fields.py` | Source-contract guards for the condensing (RHHGRC/HGRH) mirror in web/app.js |
| `tests/test_copper_strap_pricing.py` | Copper-strap price adder: $25/strap over the R-090 strap count |
| `tests/test_copper_strap_rule.py` | R-090 copper-strap rule: deterministic straps-per-header by coil type |
| `tests/test_coverage_dashboard_generator.py` | The coverage-dashboard generator must reflect the LIVE catalog, not a snapshot |
| `tests/test_deliverable_finalize.py` | DirectCoil deliverable finalize — subject cleaning, folder resolution, filing, |
| `tests/test_derive_review_surfaces.py` | TR-9: the /derive path rebuilds the Direct Coil review surfaces |

_291 source files, 201 with a header comment. 51 documented files not shown (cap 150). 90 file(s) have no header comment and appear only in the counts above — add a module docstring or leading comment and they will show up here._

<!-- FILEMAP:END -->
